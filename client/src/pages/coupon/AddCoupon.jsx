import { useState } from "react";
import { ArrowLeft, TicketPercent } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import ExploreLayout from "../../components/layout/Explore/Layout";
import ConfirmModal from "../../components/ui/ConfirmModal";
import { couponsApi } from "../../services/coupons";

const initial = { code: "", name: "", description: "", discount_type: "PERCENTAGE", discount_value: "", max_discount: "0", minimum_purchase: "0", start_date: "", end_date: "", usage_limit_total: "0", usage_limit_per_user: "0" };
const fields = [["code", "Coupon code"], ["name", "Coupon name"], ["start_date", "Start date"], ["end_date", "End date"], ["discount_value", "Discount value"], ["max_discount", "Maximum discount"], ["minimum_purchase", "Minimum purchase"], ["usage_limit_total", "Usage limit (total)"], ["usage_limit_per_user", "Usage limit per user"]];

export default function AddCoupon() {
  const navigate = useNavigate(); const [form, setForm] = useState(initial); const [error, setError] = useState(""); const [confirm, setConfirm] = useState(false); const [saving, setSaving] = useState(false);
  const change = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }));
  const validate = () => { if (!/^[A-Za-z0-9-]{3,40}$/.test(form.code)) return "Coupon code must be 3-40 characters using letters, numbers, or hyphens."; if (!form.name.trim()) return "Coupon name is required."; if (!form.start_date || !form.end_date || form.start_date > form.end_date) return "Choose a valid date range."; if (Number(form.discount_value) <= 0 || (form.discount_type === "PERCENTAGE" && Number(form.discount_value) > 100)) return "Enter a valid discount value."; if (["max_discount", "minimum_purchase", "usage_limit_total", "usage_limit_per_user"].some((key) => Number(form[key] || 0) < 0)) return "Amounts and usage limits cannot be negative."; if (Number(form.usage_limit_per_user) > 0 && Number(form.usage_limit_total) > 0 && Number(form.usage_limit_per_user) > Number(form.usage_limit_total)) return "Per-user usage cannot exceed the total limit."; return ""; };
  const submit = (event) => { event.preventDefault(); const message = validate(); setError(message); if (!message) setConfirm(true); };
  const create = async () => { setSaving(true); try { await couponsApi.create(form); navigate("/coupons", { replace: true, state: { message: "Coupon submitted and is pending admin approval." } }); } catch (requestError) { const body = requestError.response?.data; setError(body?.detail || (body && typeof body === "object" ? Object.values(body).flat().join(" ") : "Unable to create coupon.")); } finally { setSaving(false); } };
  return <ExploreLayout><main className="min-h-screen bg-[#0d1418] px-4 pb-16 pt-24 text-white sm:px-8"><div className="mx-auto max-w-5xl"><Link to="/coupons" className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-[#00ff85]"><ArrowLeft size={16} /> Back to coupons</Link><div className="mt-6 flex items-center gap-3"><TicketPercent className="text-[#00ff85]" size={28} /><div><p className="text-sm uppercase tracking-[.22em] text-[#00ff85]">Organizer workspace</p><h1 className="text-3xl font-light">Add New Coupon</h1></div></div>{error && <p className="mt-5 rounded-xl border border-red-400/30 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<form onSubmit={submit} className="mt-7 space-y-5"><section className="rounded-2xl border border-white/10 bg-[#171e25] p-5"><h2 className="border-l-4 border-[#00ff85] pl-3 font-semibold">Basic Information</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{fields.slice(0, 2).map(([name, label]) => <label key={name} className="text-sm text-slate-300">{label} *<input name={name} value={form[name]} onChange={change} className="mt-1.5 w-full rounded-lg border border-white/10 bg-[#111820] px-3 py-2.5 text-white" /></label>)}<label className="text-sm text-slate-300 sm:col-span-2">Description<textarea name="description" value={form.description} onChange={change} maxLength={300} rows="3" className="mt-1.5 w-full rounded-lg border border-white/10 bg-[#111820] p-3 text-white" /></label></div></section><section className="rounded-2xl border border-white/10 bg-[#171e25] p-5"><h2 className="border-l-4 border-[#00ff85] pl-3 font-semibold">Discount Information</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-300">Discount type *<select name="discount_type" value={form.discount_type} onChange={change} className="mt-1.5 w-full rounded-lg border border-white/10 bg-[#111820] px-3 py-2.5 text-white"><option value="PERCENTAGE">Percentage (%)</option><option value="AMOUNT">Fixed amount</option></select></label>{fields.slice(4, 7).map(([name, label]) => <label key={name} className="text-sm text-slate-300">{label}<input name={name} type="number" min="0" step="0.01" value={form[name]} onChange={change} className="mt-1.5 w-full rounded-lg border border-white/10 bg-[#111820] px-3 py-2.5 text-white" /></label>)}</div></section><section className="rounded-2xl border border-white/10 bg-[#171e25] p-5"><h2 className="border-l-4 border-[#00ff85] pl-3 font-semibold">Validity & Usage</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{fields.slice(2, 4).concat(fields.slice(7)).map(([name, label]) => <label key={name} className="text-sm text-slate-300">{label} *<input name={name} type={name.includes("date") ? "date" : "number"} min="0" value={form[name]} onChange={change} className="mt-1.5 w-full rounded-lg border border-white/10 bg-[#111820] px-3 py-2.5 text-white" /></label>)}</div></section><div className="flex justify-end gap-3"><Link to="/coupons" className="rounded-lg border border-white/15 px-5 py-2.5 text-sm">Cancel</Link><button disabled={saving} className="rounded-lg bg-[#00ff85] px-5 py-2.5 text-sm font-semibold text-black">Create coupon</button></div></form></div>{confirm && <ConfirmModal title="Submit Coupon?" message="This coupon will be sent to the admin for approval." confirmLabel="Create coupon" loading={saving} onCancel={() => setConfirm(false)} onConfirm={create} />}</main></ExploreLayout>;
}
