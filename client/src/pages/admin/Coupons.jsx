import { useEffect, useMemo, useState } from "react";
import { Eye, TicketPercent } from "lucide-react";
import { Link } from "react-router-dom";
import Layout from "../../components/layout/admin/Layout";
import { couponsApi } from "../../services/coupons";
const classes = { PENDING: "bg-amber-400/15 text-amber-300", APPROVED: "bg-emerald-400/15 text-emerald-300", REJECTED: "bg-red-400/15 text-red-300" };
export default function Coupons() { const [items, setItems] = useState([]); const [filter, setFilter] = useState("ALL"); const [loading, setLoading] = useState(true); useEffect(() => { couponsApi.admin().then(({ data }) => setItems(data.coupons || [])).finally(() => setLoading(false)); }, []); const visible = useMemo(() => items.filter((item) => filter === "ALL" || item.status === filter), [items, filter]); return <Layout><main className="min-h-screen bg-[#0f0c29] px-5 py-8 text-white sm:px-10"><div className="mx-auto max-w-7xl"><p className="text-sm uppercase tracking-[.25em] text-[#00ff85]">Discount management</p><h1 className="mt-2 text-3xl font-light">Coupons</h1><div className="mt-7 flex flex-wrap gap-2">{[["ALL", "All"], ["PENDING", "Pending Review"], ["APPROVED", "Approved"], ["REJECTED", "Rejected"]].map(([value, label]) => <button key={value} onClick={() => setFilter(value)} className={`rounded-lg px-4 py-2 text-sm ${filter === value ? "bg-[#00ff85] text-black" : "border border-white/10 text-slate-300"}`}>{label}</button>)}</div><section className="mt-5 overflow-x-auto rounded-2xl border border-white/10 bg-white/[0.04]"><table className="w-full min-w-[800px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase text-slate-500"><tr>{["Coupon", "Organizer", "Discount", "Validity", "Status", "Actions"].map((heading) => <th key={heading} className="px-5 py-4">{heading}</th>)}</tr></thead><tbody>{visible.map((coupon) => <tr key={coupon.id} className="border-b border-white/5"><td className="px-5 py-4"><p className="font-mono font-semibold text-[#00ff85]">{coupon.code}</p><p className="text-slate-300">{coupon.name}</p></td><td className="px-5 py-4 text-slate-300">{coupon.organizer_name}</td><td className="px-5 py-4">{coupon.discount_type === "PERCENTAGE" ? `${coupon.discount_value}%` : `₹${coupon.discount_value}`}</td><td className="px-5 py-4 text-slate-400">{coupon.start_date} – {coupon.end_date}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs ${classes[coupon.status]}`}>{coupon.status}</span></td><td className="px-5 py-4"><Link to={`/admin/coupons/${coupon.id}`} className="grid h-9 w-9 place-items-center rounded-lg border border-white/10 hover:border-[#00ff85]/50"><Eye size={16} /></Link></td></tr>)}</tbody></table>{loading && <p className="p-10 text-center text-slate-400">Loading coupons...</p>}{!loading && !visible.length && <p className="p-10 text-center text-slate-400"><TicketPercent className="mx-auto mb-2" />No coupons found.</p>}</section></div></main></Layout>; }
